# Set to "true" to use the backend HttpAPI; leave empty/false for the in-memory mock.
VITE_USE_BACKEND=false
# Backend base URL (no trailing slash, no /api/v1 suffix).
VITE_BACKEND_URL=http://localhost:8080
