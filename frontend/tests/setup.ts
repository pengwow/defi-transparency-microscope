// Vitest global setup.  Loaded once per test run.
// Add polyfills or global mocks here as the test suite grows.

// `cleanup` from @testing-library/react is only needed once we have React
// component tests; left as a comment for the next task to wire up.
//
// import { afterEach } from 'vitest';
// import { cleanup } from '@testing-library/react';
// afterEach(() => cleanup());

